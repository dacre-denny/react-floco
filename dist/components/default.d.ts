import * as React from "react";
/**
 * Encloses content that is rendered in a Switch block when no Case matches the current Switch value.
 *
 * @param props
 */
export declare const Default: React.SFC;
export declare const isTypeDefault: (element: React.ReactNode) => boolean;
